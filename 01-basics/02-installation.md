# Fiber ইনস্টলেশন গাইড

## Go ইনস্টল করুন

### Windows এ Go ইনস্টলেশন

1. [golang.org/dl](https://golang.org/dl/) থেকে Go ডাউনলোড করুন
2. `.msi` ফাইল রান করুন
3. ইনস্টলেশন শেষে টার্মিনাল রিস্টার্ট করুন

### ভেরিফাই করুন
```bash
go version
# আউটপুট: go version go1.21.0 windows/amd64
```

## প্রজেক্ট সেটআপ

### ধাপ ১: নতুন ফোল্ডার তৈরি করুন
```bash
mkdir my-fiber-app
cd my-fiber-app
```

### ধাপ ২: Go Module Initialize করুন
```bash
go mod init my-fiber-app
```

এটি একটি `go.mod` ফাইল তৈরি করবে:
```
module my-fiber-app

go 1.21
```

### ধাপ ৩: Fiber ইনস্টল করুন

**Fiber v3 (সর্বশেষ):**
```bash
go get github.com/gofiber/fiber/v3
```

**Fiber v2 (স্থিতিশীল):**
```bash
go get github.com/gofiber/fiber/v2
```

### ধাপ ৪: main.go ফাইল তৈরি করুন

```go
// main.go
package main

import (
    "log"
    "github.com/gofiber/fiber/v3"
)

func main() {
    // নতুন Fiber অ্যাপ তৈরি
    app := fiber.New()

    // রুট রাউট
    app.Get("/", func(c fiber.Ctx) error {
        return c.SendString("স্বাগতম Fiber-এ! 🎉")
    })

    // সার্ভার শুরু
    log.Fatal(app.Listen(":3000"))
}
```

### ধাপ ৫: অ্যাপ্লিকেশন চালান
```bash
go run main.go
```

### ধাপ ৬: ব্রাউজারে দেখুন
```
http://localhost:3000
```

## প্রজেক্ট স্ট্রাকচার

ইনস্টলেশনের পর আপনার ফোল্ডার দেখতে এরকম হবে:

```
my-fiber-app/
├── go.mod          # ডিপেন্ডেন্সি ম্যানেজমেন্ট
├── go.sum          # ডিপেন্ডেন্সি চেকসাম
└── main.go         # মূল কোড
```

## দরকারী কমান্ড

### ডিপেন্ডেন্সি আপডেট
```bash
go mod tidy
```

### সমস্ত ডিপেন্ডেন্সি ডাউনলোড
```bash
go mod download
```

### বিল্ড করুন (Executable তৈরি)
```bash
go build -o app.exe
```

### টেস্ট চালান
```bash
go test ./...
```

## Hot Reload সেটআপ (ঐচ্ছিক)

ডেভেলপমেন্টের সময় কোড পরিবর্তনে অটোমেটিক রিস্টার্ট করতে **Air** ব্যবহার করুন:

```bash
# Air ইনস্টল করুন
go install github.com/air-verse/air@latest

# প্রজেক্ট ফোল্ডারে Air চালান
air
```

## সমস্যা সমাধান

### সমস্যা: "go: command not found"
**সমাধান:** Go সঠিকভাবে ইনস্টল হয়নি। PATH এ Go যোগ করুন।

### সমস্যা: Module ডাউনলোড হচ্ছে না
**সমাধান:**
```bash
go env -w GOPROXY=https://proxy.golang.org,direct
```

### সমস্যা: Port 3000 ব্যস্ত
**সমাধান:** অন্য পোর্ট ব্যবহার করুন:
```go
app.Listen(":8080")
```

---

[← আগের: পরিচিতি](01-introduction.md) | [পরবর্তী: Hello World →](03-hello-world.md)
